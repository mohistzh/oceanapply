


<%= form_for(@request, :html => { :class => "form-horizontal" }) do |entry| %>
  <% if @request.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@request.errors.count, "error") %> prohibited this request from being saved:</h4>

      <ul>
      <% @request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= entry.label :title, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= entry.text_field :title, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= entry.label :applier, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= entry.text_field :applier, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= entry.label :filepath, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= entry.text_field :filepath, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= entry.label :reason, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
       <%= entry.text_area :reason, :class => "form-control" %>
    </div>

  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= entry.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
